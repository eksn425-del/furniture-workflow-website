# 网站实测总结与集中修复反馈

日期：2026-09-08。范围：9 月 6–7 日试用续测及此次暂停后的集中修复。

## 先说结论

现在是**可启动、已有真实模型交付证据，但仍需限定范围使用的开发验收版**，不是已经验证任意网站都能自动交付的成熟系统。固定试用等级仍为 `NOT_READY`：尚未取得三个不同站点的真实 N=1 Ready 证据。

按用户最新要求，逐站测试已经暂停。Nathan James 最后运行的 Job 已通过正式取消接口停止（CANCELLED），原身份、checkpoint、账本保留；没有新增付费建模。之后只做离线回归、传输协议检查和本地网页检查，未重新爬取源站。

重要区分：本次“代码修复及回归通过”不等于“原网站已经真实重测通过”。没有修改产品池、尺寸数据或旧扫描快照来制造成功。

## 本轮到底测试了多少

原 45 站清单中，已有 **7 个站点的本轮尝试记录，38 个未运行**。另外有 IKEA 的历史扫描，属于清单外案例，不能拿来凑 45 站覆盖率。网站库共出现 8 个站点，不等于 8 个完整成功。

| 站点 | 实际走到哪里 | 结论与未完成项 |
| --- | --- | --- |
| Interior Define | 扫描得到 36 类；Dining Tables 22；三个真实候选图已审；Nina N=1 达到 MODEL_INPUT_LOCKED | READY_ONLY。Provider OFF，未新增生成或下载 |
| Room & Board | 复用 Sawyer task 3304300，同 Job 的真实成品网页下载已验证 | 历史资产恢复交付成功；不能算新随机类目的端到端通过 |
| Alessi | 扫描与多轮工具执行；Bathroom N=1；三个真实产品图已审；停在尺寸补查 | 0 Ready、0 成品。不是“官网已证明没有尺寸”，也不是外部拒绝访问 |
| Nathan James | L1/L2 扫描，得到实际导航；L1 吧椅 86、餐椅 145；随机 Storage → Entertainment Centers，N=1 启动 | 用户暂停时正式取消，0 候选、0 成品；不把启动算通过 |
| West Elm | 实际 L2 返回 403 Restricted Access，无 CAPTCHA 控件 | ACCESS_BLOCKED；不能用代码绕过访问限制 |
| CGTrader | 历史扫描在浏览器阶段超时，无可用类目或 Ready | 共享超时实现已修，未重测；不能声称永久外部封锁 |
| Poly Haven | 历史 HTTP 扫描超时，无可用类目或 Ready | 同上，保留待验证，不算通过 |
| IKEA（额外） | 历史扫描 TEMPORARY_FAILURE | 不计入原 45 站分母，未重新测试 |

汇总口径：**新站 Ready 1 个，历史 Sawyer 恢复下载 1 件，本轮新增付费生成 0 次**。不能据此计算任意新网站成功率。逐行结果见 `WEBSITE_TEST_RESULTS_20260908.json`。早期 `trial_evidence/20260906` 是历史快照，不能再作为最新结果；其中超时被笼统标为 ACCESS_BLOCKED 的条目，以本报告纠正。

## 集中修复了什么

| 问题 | 原因/修复 | 验证边界 |
| --- | --- | --- |
| 扫描明明还在执行，却被报超时 | 前轮已删除脱离执行线程的总超时包装；保留底层请求/浏览器/大脑等待预算 | 原 Interior Define 扫描曾恢复；本次相关回归保留 |
| 备用候选卡住整个 N=1 | 前轮修复：目标候选已可推进时，备用尺寸失败不抢先阻断 | Nina 原任务 Ready 已实测 |
| 部门降成二级、平面 URL 找不到父类 | 尊重大脑审核后的部门层级；API 使用持久化 parent_category_id 解析父路径 | 离线回归；旧业务快照未改写 |
| 清理后又混回营销/风格栏目 | 完整两级审核结果不再自动补回未被采纳的规则栏目；不完整回答仍保留规则证据 | 离线回归；仍须以后真实抽核是否漏类 |
| L2 把 L1 已取得数量清空 | 同一扫描按同一产品类目 URL 合并，仅给 UNKNOWN 补回已有 EXACT/ESTIMATED 证据；不相加、不提升为已验证 | 离线回归；旧扫描仍保留历史值 |
| UNKNOWN 在目标页显示为约 0 件 | 目标页使用“未知”；顺序策略配额显示“按顺序补足”，不显示虚构的零配额 | 前端类型检查、构建；本地页面验收 |
| 约 6 分钟后页面让用户重新扫描 | 改成继续低频轮询同一个 Job，并明确后台仍进行，不引导重复创建 | 前端构建；不为验证等待再跑源站 |
| 从网站库开始任务丢失品牌设置 | 读取同站历史明确的品牌标记/品牌名，传回新任务配置 | 新增 API 回归；不凭域名猜品牌 |
| CSS 的 width=100/height=18 被当尺寸 | 前轮过滤 script/style 和无物理单位值；旧无单位数据不继续合并；补充规格表“Height (cm): 18”、显式直径解析 | 离线回归；不把测试数字写入业务 |
| 尺寸面板没展开、失败无证据 | Website 支持 More information/Product details；保存有界标题、URL、可见文字及匹配结果 | Alessi 展开动作曾实测成功，但仍没读到尺寸 |
| 一定要三轴齐全才允许建模 | 默认允许已取得官方轴作为保形缩放锚点；已有严格 FULL_ONLY 订单仍严格；官方轴优先，不用 AI 替换 | 官方单轴/AI 单轴/严格策略/挑战保护回归 |
| 没尺寸就一直等待，估计高度不能进入流程 | 有限官方补查后通过原有 Brain 请求视觉尺度；允许带明确物理单位的 AI 锚点，保留 LOOKUP_INCOMPLETE，不假称官方不存在 | 离线回归；Alessi 新估计请求未实连重测 |
| Codex 看得到本地图，公司 API 只收到路径 | 统一图片准备：检查 OUTPUT_ROOT 边界、大小、可解码、hash；远程 Chat Completions 图像内容使用原字节 data URL；bridge 保留同字节的可追溯本地引用 | 本地 PROTOCOL_TEST；公司接口是否接受 data URL、视觉效果仍待实连 |
| Blender 判断缺产品参考图 | 方向请求加入原产品 media_path，与视图同样走视觉传输校验 | 本地协议检查；未重新生成/变换 Sawyer |
| 配置了 bridge 目录就显示大脑 READY | 没有近期被接受的响应时显示“本地大脑未连接”；近期响应只表示近期有响应，不保证未来自动在线 | 状态回归及本地系统页检查 |
| 暂停后后台可能自动续扫 | 增加 WEBSITE_BACKGROUND_WORK_PAUSED，阻止后台扫描调度与生产队列提升；当前以该模式启动 | 队列不访问回归；不删除业务数据 |

没有新建 Agent Engine，没有引入 Skills，没有输出密钥，没有推送 GitHub。

## 本地验收

- 最终 API 回归 **132 passed（38.71 秒）**，覆盖扫描、类别、数量、恢复、生产安全、视觉输入等；新增集中修复回归文件 `services/api/tests/test_trial_consolidation.py`。这些是离线/协议测试，不是 132 个真实网站。
- Web `typecheck`、`lint`、生产 `build` 均执行。lint 无 error，保留 2 项既有 warning：未使用 useRef、原生 img 性能建议。
- 本地页面读取到了 API/数据库/Blender 状态；大脑断开如实显示，不再伪装有自动模型。
- 保留原 Sawyer ZIP：3646766 字节，SHA256 `156b9f2382960acd363bb98f45e964f8742d0cad776fec4e805fb4af8d37539c`。原官方尺寸 18×4×3 in；既有成品实测约 45.72×11.82×8.58 cm，宽锚点等比缩放，不声称三轴均在 5% 内。

## 当前如何查看，以及还不能保证什么

本地入口：`http://localhost:3000`。本次 API 使用原数据目录 `E:/living/closure_evidence_20260906/website_real`，后台暂停。可查看历史站点、任务和成品；**暂不点击开始生产、恢复或重新摸底**。

本机现有 API 启动器：`E:/living/run_trial_api.ps1`。审核模式先设置 `$env:WEBSITE_BACKGROUND_WORK_PAUSED='true'` 再启动；前端在 `apps/web` 运行 `E:/npm.cmd run start -- -p 3000`。恢复测试时需明确取消该环境开关并重启；这不等于会自动出现大脑，仍需活动 Codex 响应会话或已实连公司模型。

仍需后续验证，不包装成已完成：

1. 本次共享修复在 Alessi/Nathan James 原失败场景的真实恢复结果，以及图片身份绑定、命名、模型下载能否完整走通。遵照暂停要求，本次不再做这些实站测试。
2. 公司 API 的实际图像协议支持及视觉判断效果：`COMPANY_MODEL_NOT_TESTED`。本次支持的是现有兼容接口的 data URL 格式，不承诺任意供应商都支持。
3. West Elm 的真实访问条件；CGTrader/Poly Haven 修复后的真实访问结果。
4. 其余 38 个原清单站点，以及三站 Ready/三站下载的试用门。
5. 旧任务/旧类目快照显示的是当时结果；不会用离线修复伪造新的线上证据。历史状态计数与正在运行的 OS 进程须分开理解。

因此：网站比上一版补齐了多处关键连接和状态问题，可以本地查看、审查源码和已有交付；现在还不能让内部员工在没有大脑响应的情况下，把任意网址丢进去就放心无人值守批量生产。
